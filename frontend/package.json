{
  "name": "musicstore-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test-docker": "docker compose up --force-recreate --abort-on-container-exit",
    "test": "npx playwright test",
    "test-headed": "npx playwright test --headed",
    "test-ui": "npx playwright test --ui"
  },
  "dependencies": {
    "pinia": "^3.0.0",
    "vue": "^3.5.30",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "@vitejs/plugin-vue": "^6.0.5",
    "vite": "^8.0.1"
  }
}
